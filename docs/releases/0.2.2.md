# Release 0.2.2 — plan & status

Model rewriting + option / robustness fixes, plus auto-naming polish and
RuleMap token-bearing wrapper fidelity ([#33](https://github.com/miho/VMF-Text/pull/33)
folded in so this is a **single** Maven Central publish — no separate 0.2.3).
Process: [`docs/RELEASING.md`](../RELEASING.md). Full notes:
[`CHANGELOG.md`](../../CHANGELOG.md) (`## [0.2.2]`). Tracking issue:
[#29](https://github.com/miho/VMF-Text/issues/29).

- **Version:** `0.2.2` (`config/common.properties`); VMF stays `0.2.10`,
  ANTLR `4.13.2`.
- **Release commit:** tip of `main` (includes `4f36338` / #33). The earlier
  annotated tag `v0.2.2` pointed at `1a8d4a0` and must be **moved** before
  publish.
- **Headline:** parser rule maps / model rewriting (`RuleMap`,
  [#1](https://github.com/miho/VMF-Text/issues/1)); fail-fast code generation +
  tinylog logging; `superClass` option fix
  ([#14](https://github.com/miho/VMF-Text/issues/14)); aarch64 miniclang test
  guard ([#23](https://github.com/miho/VMF-Text/issues/23)); Gradle-plugin
  JDK 17 load fix; automatic naming polish (#31); RuleMap token-bearing shells
  (#32).

## Done

- [x] Parser rule maps / model rewriting `RuleMap` (#1) — see
      [`docs/RULE_MAPS.md`](../RULE_MAPS.md)
- [x] `superClass` option preserved across rule-level `options {}` (#14)
- [x] aarch64 miniclang `parseUnparseRunCodeTest` guard (#23)
- [x] Fail-fast code generation — `VMFTextGenerationException`, no partial
      output (core `GenerationFailureTest`)
- [x] Logging migrated to tinylog (quiet by default at `info`)
- [x] Gradle plugin loads under a Gradle daemon on JDK 17 (plugin Groovy → Java
      11 bytecode; Groovy `2.4.12` → `4.0.21`)
- [x] Automatic naming polish (#31) + RuleMap token-bearing shells (#32) via
      [#33](https://github.com/miho/VMF-Text/pull/33) — folded into this release
- [x] Version bump to `0.2.2`; README / examples aligned
- [x] `CHANGELOG.md` `[0.2.2]` includes #33 (dated 2026-09-17)
- [x] Release PR merged to `main`; CI green
- [x] Draft GitHub release created (notes need refresh for #33)
- [x] Merged `release/0.2.2` branch deleted (local + origin)

## Remaining (before / during publish)

- [ ] Move annotated tag `v0.2.2` to current `main` tip (includes #33); refresh
      draft GitHub release notes from `CHANGELOG.md`
- [ ] Close [#30](https://github.com/miho/VMF-Text/issues/30)–[#32](https://github.com/miho/VMF-Text/issues/32)
      (done in #33; left open because the landing PR lacked write access)
- [ ] Local full-chain validation — `sh ./build-and-test-all.sh`
      (CI already green on `main`)
- [ ] Publish `eu.mihosoft.vmf:vmf-text:0.2.2` (core) → Maven Central
- [ ] Publish `eu.mihosoft.vmf:vmf-text-gradle-plugin:0.2.2` → Maven Central
      + Gradle Plugin Portal (`eu.mihosoft.vmftext` 0.2.2 + Central marker);
      `publishPlugins` from the tag needs `-Pgroup=eu.mihosoft.vmf -Pversion=0.2.2`
- [ ] Smoke-test Central-only resolution from a clean consumer project
- [ ] Publish the drafted GitHub release once the artifacts resolve
- [ ] Post-publish: flip `CHANGELOG.md` "Publish (pending)" → "Published";
      check off the ROADMAP "Ship 0.2.2" boxes; close #29

Exact publish commands: [`docs/RELEASING.md`](../RELEASING.md) §3–4. Phase
tracking: [`ROADMAP.md`](../../ROADMAP.md) "Ship 0.2.2".
