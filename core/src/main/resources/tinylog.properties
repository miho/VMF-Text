# VMF-Text is a build-time code generator. Keep logging quiet by default and
# route everything through tinylog. Raise the level to debug/trace to see the
# per-rule / per-property generation diagnostics that used to be printed
# unconditionally to stdout.
level = info

writer         = console
writer.format  = {level}: {message}
